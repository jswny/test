on: [push]

jobs:
  notify:
    name: Notify
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Output
        run: |
          echo ${{ github.ref }}
